@model IEnumerable<ParkVehicle>

@{
    ViewData["Title"] = "Index";
}

<h1>Main menu</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RegistrationNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VehicleType)
            </th>
            <th>
                Date and Time of Parking.
            </th>
            <th>
                Time Parked
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.RegistrationNumber)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.VehicleType)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ParkingDate)
                <td>
                        @{
                            var timePassed = DateTime.Now - item.ParkingDate;
                            var hoursRoundedDown = (int)Math.Floor(timePassed.TotalHours);
                            var minutesRoundedDown = (int)Math.Floor(timePassed.TotalMinutes);
                        }
                        @hoursRoundedDown hours @minutesRoundedDown minutes
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Collect</a>
                </td>
            </tr>
        }
    </tbody>
</table>